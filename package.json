{
  "name": "slackish",
  "version": "1.0.0",
  "description": "A socket.io chat app that sort of looks like Slack",
  "main": "index.js",
  "scripts": {
    "build": "npm --prefix client run build",
    "client": "npm --prefix client start",
    "postinstall": "(cd client && yarn install); (cd server && yarn install)",
    "server": "nodemon --watch server --ignore server/src/public",
    "start": "node server"
  },
  "keywords": [],
  "author": "James Abney",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {}
}
